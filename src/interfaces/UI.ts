export interface ClientInfo {
  firstName: string;
  lastName: string;
  zipCode: string;
}

export interface UserData {
  userName: string;
  password: string;
  loginMessage: string;
}

export interface Product {
  id: string;
  name: string;
  description: string;
  price: string;
}
